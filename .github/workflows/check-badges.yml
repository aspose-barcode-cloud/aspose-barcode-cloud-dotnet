name: Check badges in README.md

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Check badges in README.md
      run: bash ./scripts/check-badges.bash
